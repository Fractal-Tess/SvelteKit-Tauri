import type { Theme } from './types';

/// <reference types="@sveltejs/kit" />
declare global {
	namespace App {
		// interface Locals {}
		// interface Platform {}
		// interface Session {}
		// interface Stuff {}
	}
}
